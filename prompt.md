请梳理当前代码的执行流程，要求：

1. 整体尽量简洁，能让我以后一眼扫清楚思路即可，不需要展开解释每个细节
2. 不要标行号，按执行顺序一步步往下讲，做成一条连贯的叙述，不要分成"步骤/设计意图/数据流转"这种独立模块
3. 每一步顺带说清楚：做了什么、为什么这么做（关键设计意图）、相关数据/变量（如消息队列）在这一步是如何被传递或修改的——都揉在这一步里讲，不单独拎出来
4. 复杂度判断：
   - 若流程是线性的（没有分支、循环、多轮交互）→ 只用文字顺序描述，不画图
   - 若涉及分支/循环/多步交互（比如工具调用循环）→ 在最后补一个简洁的 ASCII 流程图